import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function OffsetSelector() {
    return (
        <View>
            <Text>OffsetSelector</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
